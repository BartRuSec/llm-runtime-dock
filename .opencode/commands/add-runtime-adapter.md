---
description: Add a runtime adapter for another inference backend, or decide the declarative custom adapter is enough
---

Use the `add-runtime-adapter` skill. If it is not available, read
@.claude/skills/add-runtime-adapter/SKILL.md and follow it. Follow it end to end,
including its verification step, and start with Step 0: decide whether this
runtime needs a package at all.

Runtime: $ARGUMENTS
